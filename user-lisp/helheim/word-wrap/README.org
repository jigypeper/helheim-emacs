* helheim-word-wrap

This module defines ~+word-wrap-mode~ minor-mode, which intelligently wraps long
lines in the buffer without modifying the buffer content.

** Enable

No need to ~require~ this module manually. The =+word-wrap-mode= is autoloaded, and
bound to =SPC t w=.
